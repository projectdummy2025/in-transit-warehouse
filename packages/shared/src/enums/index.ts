// LPN status domain values
export enum LpnStatus {
  RECEIVED = "RECEIVED",
  STAGED = "STAGED",
  PICKED = "PICKED",
  DISPATCHED = "DISPATCHED"
}

// Location type domain values
export enum LocationType {
  INBOUND = "INBOUND",
  TRANSIT = "TRANSIT",
  OUTBOUND = "OUTBOUND"
}
