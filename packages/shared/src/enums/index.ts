// LPN lifecycle status domain values
export enum LpnStatus {
  RECEIVED = "RECEIVED",
  STAGED = "STAGED",
  PICKED = "PICKED",
  DISPATCHED = "DISPATCHED",
}

// Warehouse location classification types
export enum LocationType {
  INBOUND = "INBOUND",
  TRANSIT = "TRANSIT",
  OUTBOUND = "OUTBOUND",
}
